export class RoutineParams {
    describeLabel: string;
    itLabel: string;
  
  
    constructor(input: RoutineParams) {
      this.describeLabel = input.describeLabel;
      this.itLabel = input.itLabel;
    }
  }